import { Component, Input } from '@angular/core';

// import { User } from '../../resourses/factories';

@Component({
  selector: 'wsm-user',
  templateUrl: '_user.html',
})
export class UserPartial {
  // @Input() user: User;
  // @Input() selectMode: SelectMode;
  // @Input() openForm: Function;
  //
  // handleClick() {
  //   if (this.selectMode.enabled)
  //     this.selectMode.toggle(this.user)
  //   else
  //     this.openForm(this.user);
  // }
}
